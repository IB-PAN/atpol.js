# atpol

Work in progress. Eventually meant to end up as a library in npm registry + have some SPA app for calculations.

Conversion from ATPOL grid code into other formats is already written, but conversion from coordinates back into grid code isn't currently supported.
